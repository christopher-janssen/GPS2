# Windows Configuration for GPS2 Docker Environment
# Usage: copy .env.windows .env

# Database Configuration
POSTGRES_DB=gps_analysis
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGIS_PORT=5433

# Nominatim Configuration
NOMINATIM_PORT=8080

# Research Data Paths (Windows - mapped network drives)
# Primary research data (contains GPS data, nominatim files)
RESEARCH_DATA_PATH=S:/risk

# Additional research drive (restricted access)
RESTRICTED_DATA_PATH=R:/

# Alternative UNC paths (if mapped drives don't work):
# RESEARCH_DATA_PATH=//research.drive.wisc.edu/jjcurtin/studydata/risk
# RESTRICTED_DATA_PATH=//restricted.drive.wisc.edu/jjcurtin

# Container Platform (Windows Docker Desktop)
CONTAINER_PLATFORM=linux/amd64