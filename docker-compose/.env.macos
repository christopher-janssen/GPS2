# macOS Configuration for GPS2 Docker Environment
# Usage: cp .env.macos .env

# Database Configuration
POSTGRES_DB=gps_analysis
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGIS_PORT=5433

# Nominatim Configuration
NOMINATIM_PORT=8080

# Research Data Path (macOS - mounted volumes)
RESEARCH_DATA_PATH=/Volumes/jjcurtin/studydata/risk

# Container Platform (Apple Silicon compatibility)
CONTAINER_PLATFORM=linux/amd64